ngDescribe({
    name: 'Test reset-password component',
    modules: 'app',
    element: '<reset-password></reset-password>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
